---
// The old Settings › team page. Clinic settings is one page now; this address
// redirects to its section — People (each person's own page for their actions) — carrying its query along, and a form
// drawn before the change is re-sent there as it was. See oldSettingsPage() in
// _lib/common.ts.
export const prerender = false;
import { oldSettingsPage } from './_lib/common';

return await oldSettingsPage(Astro, 'team');
---
