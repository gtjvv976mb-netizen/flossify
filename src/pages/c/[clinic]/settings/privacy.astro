---
// The old Settings › privacy page. Clinic settings is one page now; this address
// redirects to its section — Privacy & consent — carrying its query along, and a form
// drawn before the change is re-sent there as it was. See oldSettingsPage() in
// _lib/common.ts.
export const prerender = false;
import { oldSettingsPage } from './_lib/common';

return await oldSettingsPage(Astro, 'privacy');
---
