== BUILD/FIX: The backend and shared library for the patient forms QR feature are built and applied to flossify_qr. The end-to-end test passes (22 checks).  - **Migration 028** (additive, idempotent: re-ran it inside a rolled-back transaction with no errors):   - `clinic_forms_key`: one live key per clinic; retired keys are kept.   - `patient_form`: forced RLS. The app can only select, and update the decision columns (status, patient_id, added_as, decided_by, decided_at). It cannot insert a form or change answers.   - `consent_version.kind` ('privacy' | 'treatment'), plus a new row `treatment-2026-09` for the treatment consent.   - `current_consent_version()` now returns the privacy notice only. New `current_consent_of(kind)`.   - `medical_history.form_id` and `patient_consent.form_id`, a new consent channel `'form'` and a shape check for it.   - Two security-definer functions: `patient_forms_clinic(key)` and `patient_form_submit(key, nonce, answers)`. The clinic comes from the key. The submit function checks everything again and returns 'invalid' instead of raising, so no answers reach a log.   - `retention_purge()` now also deletes forms nobody added, 30 days after they were sent. - **One new 
   concerns: 1. **Wording to review.** The treatment consent (`treatment-2026-09`) is my plain summary of the usual Philippine dental consent; a dentist and the owner's lawyer should read it. The privacy notice (privacy-2026-09) doesn't mention what the forms now collect (health history, address, Facebook, PhilHealth PIN), so a new notice version is needed. Both are in CLAUDE.md's Open list.  2. **`readConsents()` changed.** It now returns privacy-notice consents only, joined on `consent_version.kind = 'privacy'`. Treatment consents appear only through `patientForms()`. `current_consent_version()` also filters by kind now. Without that filter, the new treatment row (effective 26 Sep, later than the privacy notice's 23 Sep) would have become "the notice in force".  3. **Inbox not wired.** I didn't touch
== BUILD/FIX: I built the workspace side of the patient-forms QR feature, and it works end to end on the dev server against flossify_qr. Owner and desk see the same thing. A staff member without "can edit records" does not see the QR page or the queue. Another clinic's staff are refused.  **Add patient (a third way)** - The top of the page now has three choices: "Type it in" (this page), "Import a spreadsheet", and "Patients fill it in" (the QR code for the desk). - When forms are waiting, a blue line says so, so nobody types the same patient in twice.  **QR code page (`/c/<slug>/patients/qr/`)** - Shows the poster as it prints, with one teal button, Print poster. Next to it: Download QR (PNG), Copy link, and "See what patients see". - Also shows how many forms are waiting, and "Make a new QR code". That asks first, in a side panel, or inline with `?confirm=new` when scripts are off. It retires the old key; I tested this at Leonard Wood, and the old link now shows "replaced". - The poster: the Flossify mark with "flossify.ph", "Welcome to <clinic>", "New patient? Scan to fill in your forms.", the big QR (error correction H, mark in the centre, quiet zone), the short link, three small steps, and 
   concerns: 1. **Hidden-frame printing was not possible.** The middleware's `frame-ancestors 'none'` blocks it, and I did not relax the header. Print poster prints the QR page itself instead: a paper copy of the poster is moved to the end of `<body>`, and print rules scoped with `:has()` hide everything else. The `print/` page stays for direct links, and is where the button goes without scripts.  2. **Who can read an added form.** A waiting form needs `can_edit_records`. An added form is readable, and printable, by anyone who may open the branch's records, because it is part of the record like the Health tab. Every seeded role has `can_edit_records` today. If the owner wants added forms locked to editors too, it is one condition in `forms/[form]/index.astro` and `print.astro`.  3. **Shared files I cha
== BUILD/FIX: The public patient forms page is built and works end to end on flossify_qr: an adult, a child signed by a parent, forms with errors, a form sent with JavaScript off, and a refresh after sending. I changed nothing in src/lib or anywhere outside src/pages/f/, and nothing was committed, pushed or rebuilt.  **What a patient sees at /f/<key>/** - **Look:** compact frosted glass over the clinic's own photo, or the soft blur when it has none. It uses the booking's ClinicRoom, clinic-glass.css and the `cl-glass cl-book` column, and the same five-circle stepper. On a phone the step names shorten to one word each (You · Health · Teeth · Cards · Consent) so none wraps. - **Steps:** all five are drawn from STEPS in the backend's form definition.   - Sections sit inside each step.   - Yes/No/Not sure answers are 46px pills with a round dot, and on wide screens they sit beside the question.   - Allergies, conditions and "what's bothering you" are a checklist in 2 columns on a phone and 3 on a wide screen, with "None" across the full width. Ticking "None" clears the other ticks, and ticking anything else clears "None".   - Fields use 16px text and are 48px tall. Only optional questions are marked
   concerns: 1. **I briefly changed a clinic the other builder is using.** Leonard Wood's photos were switched to a test upload for about five minutes and then put back exactly (`{handpiece,tray}`). I hadn't seen that Leonard Wood already had a live key they made at 20:02 (a666bjqwaa). Its keys and forms were never touched. The other temporary test uploads and keys (Marikina, La Trinidad) and the uploads/ folder are gone.  2. **Test data left for the queue builder:** three new forms at Session Road made through the real page:    - QR-J7FQ: an adult with full answers.    - QR-2ADV: a child signed by his mother.    - QR-67JC: sent with JavaScript off.     I deleted my 15 duplicate test forms and my throttle rows (127.0.0.1, my test mobiles, the test keys). I left untouched the two backend samples, QR-B2G
== REVIEW/VERIFY pass False issues 5
   - must | src/lib/patient-forms.ts:651-655 (addToPatient, the medical_history insert; the same answe | When a form is added to a patient on file, its health version is written with answered_at = the time the form was sent. If the desk saved a health version for that patient after the form was sent but before adding it, the desk's version stays the current health and the patient's own allergies go int
   - should | src/data/migrations/028_patient_forms.sql:316-320 and :342-344 (patient_form_submit, the ' | A resend is treated as the same form ('again') whenever the nonce and the mobile match, even if the other answers are different. A parent sends one child's forms, goes back with the browser (a page restored from bfcache keeps the hidden form_nonce), changes the name and birth date for a sibling on t
   - should | src/pages/c/[clinic]/patients/[patient].astro:500 and :817; src/lib/patient-forms.ts:459-4 | After a form is added, the record claims consents that may not have been written. The success line always says 'the health history and both consents from the form'. The Patient forms list shows 'Privacy notice already on file' whenever the form wrote no privacy row. writeConsents returns 'minor' and
   - should | src/lib/patient-forms.ts:135-136 (lookupForms) and :183 (submitForms per-IP key) | Once one address has 20 missed keys in an hour, lookupForms answers 'wait' for every key from that address, valid ones included. A clinic's shared Wi-Fi, or a carrier CGNAT address shared by many phones, can then lock every patient out of a real poster for up to an hour. The 40-an-hour per-IP send l
   - should | src/pages/c/[clinic]/patients/qr/print.astro:5-7; src/lib/qr.ts:38 | Two small leftovers. print.astro's header comment still says the QR page prints it through a hidden frame, but the builder's notes and CLAUDE.md both say that cannot work (frame-ancestors 'none'). markSvg is exported but nothing uses it (the poster draws its own 'mark' op). That is dead code, agains
== REVIEW/VERIFY pass False issues 14
   - must | src/lib/patient-forms-def.ts:110-148 (CONDITIONS) + src/pages/f/_Field.astro:54 + src/page | The medical checklist is hard work on a phone. It lists 35 conditions in one flat list, in the old paper order (the heart items are split up: heart surgery, heart attack, thyroid, heart disease, heart murmur). The shortcut most patients need, "None of these", is the very last box, after all 35 ticks
   - should | src/pages/c/[clinic]/patients/forms/[form]/index.astro:264 and :275 (decision card) | When a likely match is on file, even a strong one ("Same name and birth date"), the one teal main action is still "Add as a new patient". "Add to Maria" is a small quiet button. A busy desk presses the teal button, and that makes duplicate patients.
   - should | src/pages/c/[clinic]/patients/qr/_poster.ts:135-136, 170-184 | The poster is attractive and clearly Flossify, and its QR is large with a proper quiet zone. But the three steps, the "Can't scan?" line and the brand are too small to read from a metre. The lower third looks like fine print next to the 15mm headline.
   - should | src/pages/c/[clinic]/patients/new.astro:~322-350 (fm-ways) | The QR option is there, but it does not read as the easy way. It is the third of three equal cards, after "Import a spreadsheet", with a long hint.
   - should | src/pages/c/[clinic]/patients/qr/index.astro:102-122 + _forms/forms.css:50-56 | On a phone, the page's one main action (Print poster) is below the fold, under the poster preview. The three buttons stack at uneven widths, and the intro is a 6-line paragraph.
   - should | src/pages/f/[key].astro:204 (pf-intro) | The header promises "Five short steps", but the health step is over 3 phone screens. The two-line intro also repeats on every step and pushes each step's content about 60px down.
   - should | src/lib/patient-forms-def.ts:296, 299-300, 318-330 | Step 1 is long, with 17 fields over 2.5 screens, and part of it does not suit children. The section inside "About you" is titled just "You". Civil status and occupation still show for a child. A parent types themselves twice, as emergency contact and again as guardian.
   - should | src/pages/f/_Field.astro:52 (rows rule) — last_visit, sex, nervous | Radio groups with 4–6 medium-length answers become a ragged staircase of pills on a phone. "When was your last dental visit?" is six pills of different widths, one per line.
   - should | src/pages/c/[clinic]/patients/_forms/Answers.astro:31-38 (titles come from STEPS) | The staff review page and the printout reuse the patient's own wording ("About you", "Your health", "Your teeth", "Your name", "You"). The desk is reading about someone else, so the second person reads oddly.
   - should | src/pages/c/[clinic]/patients/forms/index.astro:111, 118 | The queue is harder to scan than it needs to be. "Under 18" is an amber warning chip, although a child patient is not a problem. "Kept until 26 Oct" repeats on every new row, although the lede already says 30 days. There is no quick way to find the form a patient at the desk names by reference ("QR-
   - should | src/lib/patient-forms-def.ts:750 (answerText checks) — shown on the review page and the re | Lists join labels that each start with a capital, so a capital letter appears mid-sentence.
   - should | src/pages/c/[clinic]/patients/[patient].astro (the "form says something different" callout | After "Add to <patient>", each difference (for example a new mobile) is only listed, with "change it in Edit details". The desk has to retype it by hand, which is the typing the QR code was meant to save.
   - should | src/pages/f/[key].astro:212-218 (stepper) | Steps already done cannot be tapped to go back and fix something, so a patient on step 5 has to press Back four times. The booking's stepper has the same limit, but this form is much longer.
   - should | src/pages/f/[key].astro:334, 349 | The "Call <number>" buttons on the replaced and not-open screens use the chat-bubble icon. It reads as "message", not "call".
== REVIEW/VERIFY pass False issues 9
   - must | src/pages/f/[key].astro:219 (the <form class="cl-form pf-form" method="post" …>) and the d | After the thank-you screen, pressing Back brings the previous person's full answers back into the form. That includes name, address, mobile, the typed signature, both consent ticks and health ticks such as "HIV or AIDS". Anyone using a shared device sees them: a clinic tablet at reception, or a phon
   - must | src/pages/privacy.astro:60-64 ('What we keep') + PRIVACY_TICK in src/lib/patient-forms-def | The forms collect sensitive personal information under the Data Privacy Act. That includes a full health history (HIV/AIDS, STD, pregnancy, medicines), home address, an emergency contact, Facebook, a PhilHealth PIN, and a parent's details for a minor. Patients consent with "I have read the privacy n
   - should | src/lib/patient-forms.ts:135-136 (lookupForms peeks forms:miss:<ip> before resolving the k | After 20 missed links in an hour from one address, every /f/ request from that address gets 429 for an hour, even for the clinic's valid key. Two groups share one address: patients on the clinic's Wi-Fi, and many Philippine mobile subscribers behind carrier-grade NAT (CGNAT, many phones on one publi
   - should | 028_patient_forms.sql:323 (500 'new' cap per clinic) + LIMITS.forms.key [300/day] + forms/ | Anyone who photographs the desk poster can flood a clinic. With a few addresses and made-up 09… numbers they can use up the 300 forms a day per poster, after which real patients are told 'a lot of forms waiting'. Then they can push the clinic to 500 waiting forms. Making a new QR code does not clear
   - should | 028_patient_forms.sql:74 (clinic_forms_key grants); consent_version grants (012 default pr | The app role's grants are wider than it needs. flossify_app can UPDATE any column of clinic_forms_key, including key and retired_at. That means it could un-retire a key, and 'a retired key accepts nothing' is enforced only by app code. flossify_app also still has INSERT, UPDATE and DELETE on consent
   - should | src/lib/patient-forms.ts:646-655 (addToPatient health version) | Adding a form to a patient already on file merges allergies, but it replaces conditions and medications with whatever the form says. If the form leaves out a documented condition (for example a bleeding disorder) or a medicine such as an anticoagulant, it disappears from the current health version t
   - should | src/lib/patient-forms.ts:551-570 (FILLABLE: phone, email) used by addToPatient | 'Add to <patient>' silently copies an unverified public form's mobile and email into an existing record wherever those are empty. Someone who knows a patient's name and birth date, and whose record has no mobile (common after a paper import), gets a 'Same name and birth date' match. If the desk clic
   - should | src/middleware.ts (no Cache-Control on /c/ pages); new pages forms/[form]/index.astro, for | The review page and the printout show a full health questionnaire, including HIV/STD and pregnancy answers, but they are sent with no Cache-Control. On a shared clinic PC the browser's disk cache and back/forward history keep them after sign-out. The problem already exists across the workspace, but 
   - should | src/lib/health.ts:146 oneLine (used by parsePatientForm for every text field) | oneLine strips only control characters and DEL. Bidi overrides (U+202A–202E, U+2066–2069) and zero-width characters (U+200B–200F, U+FEFF) are kept. A form can therefore show a spoofed name in the queue and on the record, and a zero-width character inside a name breaks nameKey matching, which leads t
== REVIEW/VERIFY pass True issues 3
   - should | src/pages/f/[key].astro, the page script's refresh() (about lines 416-425: `if (minor && ! | If the date of birth is a minor's even for a moment, the script picks "A parent or guardian". When the date then changes to an adult's, that choice stays. "Myself" comes back but is not chosen. An adult can then sign with the guardian option already chosen on step 5. The server accepts it, so the co
   - should | src/lib/patient-forms-def.ts, section 'pregnancy' (about line 366, showIf: NOT_MALE); also | The pregnancy, breastfeeding and birth-control-pill questions are required for every patient who is not male, whatever the age. A parent filling in for a 6-year-old girl cannot continue until they say whether she is pregnant, breastfeeding or on the pill. It is awkward, and it goes against the owner
   - should | src/pages/f/[key].astro, done screen (line ~313, `Thank you, {done.firstName}.`) | When a parent signs for a child, the done screen thanks the child, e.g. "Thank you, Jericho." for a 7-year-old, though the parent is holding the phone. The words are right for adults and a little odd for a family.
== BUILD/FIX: I fixed all 31 round-1 findings: every must and every should. I rejected none of them. The main results:  - **The forms are now closed on flossify.ph.** Nothing lists a privacy notice that covers the forms, so there `/f/` shows "not open just now" and nothing can be sent. The QR page tells the clinic why. On a development machine they stay open. - **31 of 31 backend checks pass**, up from 22. - **The poster still decodes** to the clinic's exact link. - **All 144 contrast measurements on the public page pass.** Two runs first failed and passed on rerun; they had run while I was editing files.  Nothing was committed or pushed, and the branch is still patient-qr-forms.  **Musts** - **Back after the thank-you** no longer brings the last person's answers back, with or without JavaScript:   - The form has `autocomplete="off"`.   - A page the browser shows again from memory, or an old post it sends again, is hidden and reloaded empty.   - Pages drawn by a post turn into plain page loads in the history.   - "Fill in forms for someone else" replaces the thank-you in the history. - **Privacy notice.** On the live site the forms stay closed until a notice listed in `FORMS_PRIVACY_VERSIONS` is
   concerns: 1. **The patient forms are off on flossify.ph until a new privacy notice is published.** The owner's lawyer should read it first. `FORMS_PRIVACY_VERSIONS` is empty. To open them, in one change: add a `consent_version` migration, change `/privacy/`'s words and its hard-coded version, and add the new id to that list. The notice must name health and dental history, home address, emergency contact, a parent's details, Facebook, PhilHealth PIN and HMO card, and the 30-day deletion. The treatment consent wording (`treatment-2026-09`) still needs a dentist and the lawyer to read it.  2. **The health step is longer on a phone until "none" is ticked.** It is 3,454px with nothing ticked, against 2,884px before; ticking "none" folds each list away (1,637px with both folded). If the owner wants it sho
== REVIEW/VERIFY pass False issues 4
   - must | src/pages/c/[clinic]/patients/_forms/forms.css:183 (.fm-flags .ws-chip), used by src/pages | On the form review page, the chips in the "For the dentist first" row do not wrap. They inherit white-space: nowrap from .ws-chip (src/styles/global.css:2050). A realistic guardian name or medicine line pushes the chip past the card, the text is cut off at the card edge, and the whole page scrolls s
   - should | src/pages/c/[clinic]/patients/_ui/patients.css:133-134 (.rec-alerts .ws-chip) on the patie | The record's allergy and medicine chips have the same nowrap problem. The component is not new, but the patient forms now let patients type medicine lines of up to 60 characters, and those land in these chips. A record added from a form can therefore scroll sideways on a phone.
   - should | src/lib/patient-forms.ts:144 (FORMS_PRIVACY_VERSIONS = []) and formsNoticeReady() | On a production server the forms are closed until a new privacy notice is published and listed. So after merging, every scan of a poster on flossify.ph shows "The forms are not open just now — ask the desk for a paper form". This is deliberate and documented (Data Privacy Act informed consent; CLAUD
   - should | src/pages/f/_Field.astro (autocomplete={f.autocomplete ?? 'off'}) with the autocomplete to | The docs say autocomplete="off" on the form keeps one person's answers from reaching the next person on a shared clinic tablet. But each field's own token overrides the form-level off, and many fields have tokens: given-name, family-name, bday, tel, email, street-address, address-level1/2, organizat
== BUILD/FIX: **The patient forms will not work on the live site yet, and this is on purpose.** When the code runs on the live server (flossify.ph), scanning a poster shows "The forms are not open just now — ask the desk for a paper form". The forms stay closed until a new privacy notice is published, because patients must agree to a notice that names what the forms collect (Data Privacy Act, informed consent). The code confirms this: `FORMS_PRIVACY_VERSIONS = []` and `formsNoticeReady()` in src/lib/patient-forms.ts, and backend check 28. The clinic's QR page already says it. Four things open the forms: 1. A new `consent_version` migration whose notice covers health and dental history, home address, emergency contact, parent or guardian details, Facebook, the PhilHealth PIN and HMO card, and that a form nobody adds is deleted after 30 days. 2. New wording on the /privacy/ page, plus the version id that page has written into its code. 3. That new id added to `FORMS_PRIVACY_VERSIONS`. 4. A review by the owner's lawyer and a dentist, including the treatment consent (`treatment-2026-09`).  CLAUDE.md already covers all of this under "Closed in production" and in the Open list. On a development machin
   concerns: 1. **The live site stays closed** until a new privacy notice is published (see the summary): a new consent_version migration, the /privacy/ wording and the version id written into that page, the id added to FORMS_PRIVACY_VERSIONS, and a lawyer's and a dentist's review, including of treatment-2026-09.  2. **Autofill is the owner's call.** The contact fields keep their autofill tokens so patients fill in faster on their own phones. On a shared tablet, a browser that saves addresses could suggest one person's contact details to the next. The QR page tells a clinic tablet to use a private tab. To give up the fast fill-in instead, remove the `autocomplete` tokens from STEPS in src/lib/patient-forms-def.ts; `_Field` then renders "off" on every field.  3. **Shared styles I changed.** Besides the 
